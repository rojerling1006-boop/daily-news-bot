name: Daily News Bot 🤖

on:
  schedule:
    # 設定每天 UTC 時間 00:00 執行 (台灣時間早上 08:00)
    - cron: '0 0 * * *'
  # 讓我們可以手動按按鈕測試，不用傻傻等明天
  workflow_dispatch:

jobs:
  run-news-bot:
    runs-on: ubuntu-latest  # 借用一台最新的 Linux 電腦

    steps:
      - name: Check out code (下載程式碼)
        uses: actions/checkout@v3

      - name: Set up Python (安裝 Python)
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install libraries (安裝需要的套件)
        run: |
          pip install requests beautifulsoup4 deep-translator

      - name: Run bot (執行程式)
        run: python main.py

      - name: Upload Report (保存新聞檔案)
        uses: actions/upload-artifact@v4
        with:
          name: daily-news-report
          path: news_report.txt
